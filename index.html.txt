

/*function success(position) {
    var latitude  = position.coords.latitude;
    var longitude = position.coords.longitude;
    console.log(latitude, longitude);
    }
navigator.geolocation.getCurrentPosition(success);
  */
 
var tweet=prompt("write ur tweet?");
tweetCount=tweet.length;

var firstChar=tweet.slice(0,1);
var uppercasefirstChar=firstChar.toUpperCase();
var restofTweet=tweet.slice(1,tweet.length);
var totaltweet= uppercasefirstChar+restofTweet;
alert(totaltweet+" you have returned "+tweetCount+" characters "+ " you were left with "+  ( 100-tweetCount)+" characters remaining.");

