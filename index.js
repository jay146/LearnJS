

document.querySelector("button").addEventListener("click",handleClick);
//addEventListener() takes 2 parameters type(which we take as  Mouse Event (click)) 
//and 2 parameter listner which is a js function called when click events detected.  
function handleClick(){
alert("i got clicked");
}